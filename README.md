# Using README as a log of updates for the midterm as well

# Realized I cannot start with the REPL (I'd originally thought to begin with displaying operations only) - as i first need to create the operations, which requires other initialization.